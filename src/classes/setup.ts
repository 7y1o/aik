namespace Aik {
    export class Setup {
        /** Run setup */
        public static run() {
            let templates: any = Aik.Data.LoadJSON(`${__dirname}/../../data/templates.json`);


        }
    }
}